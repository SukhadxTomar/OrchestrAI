"""Application layer: use cases and ports.

Ports are Protocols (contracts) that the application defines and
infrastructure implements. Dependencies point inward: this layer may import
domain, never infrastructure.
"""
